global:
  mlp:
    encryption:
      key: "encryption-key"

turing:
  uiConfig: null # Not needed as we only test API
  # testing of openapi overriding feature
  openApiComponentOverrides:
    components:
      schemas:
        ExperimentEngineType:
          enum:
          - nop
          - proprietary

postgresql: &postgresql
  persistence:
    enabled: false

merlin:
  postgresql: *postgresql

mlp:
  postgresql: *postgresql
